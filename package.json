{
  "name": "metaport",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MetaPort — realidade mista no navegador/Android: VR Box SBS, hand tracking com contorno, app store e API própria.",
  "scripts": {
    "assets": "node scripts/fetch-assets.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "node scripts/serve-dist.mjs",
    "android:sync": "node scripts/sync-android.mjs",
    "android:debug": "npm run build && npm run android:sync && cd android && ./gradlew assembleDebug",
    "smoke": "node scripts/smoke.mjs",
    "lint": "eslint .",
    "check": "npm run lint && npm run smoke && npm run build",
    "serve:dist": "node scripts/serve-dist.mjs",
    "android:release": "npm run build && npm run android:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.14",
    "three": "0.169.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "globals": "^15.15.0",
    "vite": "5.4.10"
  }
}
