<?xml version="1.0" encoding="utf-8"?>
<!-- Host layout. The GLSurfaceView is the Cardboard stereo canvas; the WebView
     is a hidden JS runtime (the VR "brain"). A tiny status TextView carries
     diagnostic text without breaking the stereo render. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/vr_black"
    android:keepScreenOn="true">

    <android.opengl.GLSurfaceView
        android:id="@+id/gl_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Invisible JS runtime. Never drawn: it only executes the VR logic. -->
    <WebView
        android:id="@+id/js_runtime"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="invisible"
        android:importantForAccessibility="no" />

    <TextView
        android:id="@+id/status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:padding="6dp"
        android:textColor="#AAFFFFFF"
        android:textSize="11sp"
        android:text="" />
</FrameLayout>
